# Slackbot template

## What is this?

This is a boilerplate that can be cloned for each slackbot.

It contains e.g. the dependencies necessary for local development.

Change `signature_type` in `package.json` to `event` if the function is triggered by Pub/Sub.
